{
    attributes = (
        {
            allowsNull = N; 
            columnName = "CREATION_DATE"; 
            name = creationDate; 
            prototypeName = dateTime; 
        }, 
        {
            allowsNull = N; 
            columnName = "GROUP_PKEY"; 
            name = groupPKey; 
            prototypeName = dbKey; 
        }, 
        {
            allowsNull = N; 
            columnName = "GROUP_NAME"; 
            name = name; 
            prototypeName = shortString; 
        }, 
        {
            columnName = "OID_SECTION_STYLE"; 
            name = oidSectionStyle; 
            prototypeName = dbKey; 
        }, 
        {columnName = secret; name = secret; prototypeName = longString; }, 
        {allowsNull = N; columnName = TYPE; name = type; prototypeName = tinySting; }, 
        {
            allowsNull = N; 
            columnName = "PARENT_WEBSITE"; 
            name = websiteFKey; 
            prototypeName = dbKey; 
        }
    ); 
    attributesUsedForLocking = (groupPKey, name, oidSectionStyle, secret, type); 
    className = "com.gvcsitemaker.core.RemoteParticipantGroup"; 
    classProperties = (
        creationDate, 
        dataAccessNotifications, 
        files, 
        name, 
        oidSectionStyle, 
        parentWebsite, 
        secret, 
        sectionStyle, 
        sections, 
        type
    ); 
    externalName = "SM_GROUP"; 
    fetchSpecificationDictionary = {}; 
    internalInfo = {
        "_clientClassPropertyNames" = (creationDate, files, parentWebsite, sections); 
    }; 
    isAbstractEntity = N; 
    maxNumberOfInstancesToBatchFetch = 50; 
    name = RemoteParticipantGroup; 
    parent = WebsiteGroup; 
    primaryKeyAttributes = (groupPKey); 
    relationships = (
        {
            definition = "groupDataAccessNotifications.dataAccess"; 
            name = dataAccessNotifications; 
            numberOfToManyFaultsToBatchFetch = 50; 
        }, 
        {
            definition = "groupFiles.file"; 
            name = files; 
            numberOfToManyFaultsToBatchFetch = 50; 
        }, 
        {
            deleteRule = EODeleteRuleCascade; 
            destination = DataAccessWebsiteGroupNotification; 
            isToMany = Y; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = groupPKey; sourceAttribute = groupPKey; }); 
            name = groupDataAccessNotifications; 
            numberOfToManyFaultsToBatchFetch = 50; 
            propagatesPrimaryKey = Y; 
        }, 
        {
            deleteRule = EODeleteRuleCascade; 
            destination = GroupFile; 
            isToMany = Y; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = groupPKey; sourceAttribute = groupPKey; }); 
            name = groupFiles; 
            numberOfToManyFaultsToBatchFetch = 50; 
            propagatesPrimaryKey = Y; 
        }, 
        {
            deleteRule = EODeleteRuleCascade; 
            destination = GroupSection; 
            isToMany = Y; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = groupPKey; sourceAttribute = groupPKey; }); 
            name = groupSections; 
            numberOfToManyFaultsToBatchFetch = 50; 
            propagatesPrimaryKey = Y; 
        }, 
        {
            destination = Website; 
            isMandatory = Y; 
            isToMany = N; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = websitePKey; sourceAttribute = websiteFKey; }); 
            name = parentWebsite; 
        }, 
        {
            destination = SectionStyle; 
            isToMany = N; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = oid; sourceAttribute = oidSectionStyle; }); 
            name = sectionStyle; 
        }, 
        {
            definition = "groupSections.section"; 
            name = sections; 
            numberOfToManyFaultsToBatchFetch = 50; 
        }
    ); 
    restrictingQualifier = "(type = 'Remote Participant')"; 
}
