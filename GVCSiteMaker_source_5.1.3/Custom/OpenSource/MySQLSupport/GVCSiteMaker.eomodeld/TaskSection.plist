{
    attributes = (
        {
            allowsNull = N; 
            columnName = "SECTION_ID"; 
            name = sectionId; 
            prototypeName = dbKey; 
        }, 
        {columnName = "TASK_ID"; name = taskId; prototypeName = Key; }
    ); 
    attributesUsedForLocking = (taskId, sectionId); 
    className = "com.webobjects.eocontrol.EOGenericRecord"; 
    classProperties = (task, section); 
    externalName = "TASK_SECTION"; 
    fetchSpecificationDictionary = {}; 
    name = TaskSection; 
    primaryKeyAttributes = (taskId, sectionId); 
    relationships = (
        {
            destination = Section; 
            isToMany = N; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = sectionPKey; sourceAttribute = sectionId; }); 
            name = section; 
        }, 
        {
            destination = Task; 
            isToMany = N; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = taskId; sourceAttribute = taskId; }); 
            name = task; 
        }
    ); 
}
