In order to run the tests, do the following:
- some tests only pass when instrumented for DBC
- the ModelConnector tests require that EOFExtension's jar is on the classpath -- the default run classpath will not work
