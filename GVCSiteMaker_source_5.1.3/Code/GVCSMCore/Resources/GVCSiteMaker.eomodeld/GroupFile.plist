{
    attributes = (
        {
            allowsNull = N; 
            columnName = "FILE_PKEY"; 
            name = filePKey; 
            prototypeName = dbKey; 
        }, 
        {
            allowsNull = N; 
            columnName = "GROUP_PKEY"; 
            name = groupPKey; 
            prototypeName = dbKey; 
        }
    ); 
    attributesUsedForLocking = (filePKey, groupPKey); 
    className = "com.webobjects.eocontrol.EOGenericRecord"; 
    externalName = "SM_GROUP_FILE"; 
    fetchSpecificationDictionary = {}; 
    maxNumberOfInstancesToBatchFetch = 50; 
    name = GroupFile; 
    primaryKeyAttributes = (filePKey, groupPKey); 
    relationships = (
        {
            destination = SiteFile; 
            isToMany = N; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = filePKey; sourceAttribute = filePKey; }); 
            name = file; 
        }, 
        {
            destination = Group; 
            isToMany = N; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = groupPKey; sourceAttribute = groupPKey; }); 
            name = group; 
        }
    ); 
}
